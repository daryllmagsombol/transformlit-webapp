# name: Deploy Web

# on:
#   push:
#     paths:
#       - 'apps/web/**'
#       - 'packages/shared/**'
#     branches: [main]
#   workflow_dispatch:

# permissions:
#   contents: read
#   packages: write
#   id-token: write

# env:
#   ENVIRONMENT: dev

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup pnpm
#         uses: pnpm/action-setup@v4
#         with:
#           version: 10

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: 'pnpm'

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Build
#         run: pnpm turbo run build --filter=@transformlit/web...

#       - name: Login to GHCR
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Build and push Docker image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: apps/web/Dockerfile
#           push: true
#           tags: |
#             ghcr.io/${{ github.repository_owner }}/transformlit-web:latest
#             ghcr.io/${{ github.repository_owner }}/transformlit-web:${{ github.sha }}

#       - name: Azure login (OIDC)
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

#       - name: Deploy to Container Apps
#         uses: azure/container-apps-deploy-action@v2
#         with:
#           imageToDeploy: ghcr.io/${{ github.repository_owner }}/transformlit-web:${{ github.sha }}
#           containerAppName: ca-transformlit-web-${{ env.ENVIRONMENT }}
#           resourceGroup: rg-transformlit-${{ env.ENVIRONMENT }}
